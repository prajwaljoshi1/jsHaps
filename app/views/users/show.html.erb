<h2 align="center"> Welcome to <%= @user.username %>'s page</h2>
<div class="row">
  <h4 class="col-xs-offset-2"> <%= @user.username %>'s articles: </h4>
  <div class="center">
    <%= will_paginate @user_articles %>
  </div>
   <%= render 'articles/articles_list', obj: @user_articles  %>
   <div class="center">
     <%= will_paginate @user_articles %>
   </div>
</div>

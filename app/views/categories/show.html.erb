<h2 align="center"><%= "Category: #{@category.name} " %></h2>

<div align="center">
  <%if logged_in? && current_user.admin?  %>
    <h4 class="badge"> <%= link_to "Edit Category Name", edit_category_path(@category)  %></h4>
  <% end %>
  <%= will_paginate @category_articles %>
  </div>
  <%= render 'articles/articles_list', obj: @category_articles %>

  <div align="center">
    <%= will_paginate @category_articles %>
    </div>
